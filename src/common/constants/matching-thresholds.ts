/**
 * Shared thresholds for LLM-based contract matching alignment.
 * Used by OutcomeDirectionValidator and AuditRevalidationService to ensure
 * consistent alignment scoring across the discovery pipeline and audit pipeline.
 */
export const LLM_ALIGNMENT_THRESHOLD = 60;
